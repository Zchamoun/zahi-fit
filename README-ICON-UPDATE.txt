Zahi Fit — New App Icon Update

Upload BOTH PNG files to the ROOT of the GitHub repository:
- icon-192.png
- icon-512.png

GitHub should replace the existing files with the same names.
No manifest.json or index.html change is required because the current app
already references these filenames.

After committing, allow GitHub Pages to deploy. Installed PWA icons can remain
cached by Android; if the old home-screen icon remains after the website has
updated, remove the installed Zahi Fit shortcut/app and install it again.
